version = "1.0.0"
hardware_targets = ["linux"]
builtin = true
# Host playback can act as a sink-authored clock under `playback` mode.
capabilities = ["audio.sample", "presentation.clock"]

[[ports]]
name = "audio"
direction = "input"
content_type = "AudioSample"
required = true

[[params]]
name = "mode"
tag = 10
type = "enum"
values = ["wav", "raw", "null", "playback"]
default = "wav"

[[params]]
name = "path"
tag = 11
type = "str"
default = "./target/host-audio/out.wav"

[[params]]
name = "sample_rate"
tag = 12
type = "u32"
default = 48000
range = [8000, 192000]

[[params]]
name = "channels"
tag = 13
type = "u8"
default = 2
range = [1, 8]

# Module-scope observability — exempt (see standards/observability.md §6).
[observability]
exempt = "Linux-host hardware shim — audio/display/MIDI via the host OS; observed via host tooling, not an on-silicon data path"
